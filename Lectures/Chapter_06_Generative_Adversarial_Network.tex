%%----------------------------------------------------------------------------------------
%	PACKAGES AND THEMES
%----------------------------------------------------------------------------------------
\PassOptionsToPackage{table}{xcolor}
\documentclass[aspectratio=169,xcolor=dvipsnames,svgnames,x11names,fleqn]{beamer}
% \documentclass[aspectratio=169,xcolor=dvipsnames,fleqn]{beamer}

\usetheme{RedVelvet}

\usefonttheme[onlymath]{serif}
\newcommand{\showanswers}{yes}
\usepackage{pifont} % For checkmarks and crosses

\usepackage{setspace}
\usepackage{xspace}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{color}
\usepackage{physics}
% \usepackage{mathbb}
\usepackage{rahul_math}
\usepackage{bigints}

\usepackage{graphicx} % Allows including images
\usepackage{booktabs} % Allows the use of \toprule, \midrule and \bottomrule in tables
\usepackage{tikz,pgfplots}
\usepackage{mathtools}
\usepackage{subfigure}
\usetikzlibrary{arrows}
\usepackage{minted}
\definecolor{LightGray}{gray}{0.9}
\definecolor{cream}{rgb}{0.92, 0.9, 0.55}
\definecolor{lightblue}{rgb}{0.68, 0.85, 0.9}


\usepackage{xcolor-material}
\usetikzlibrary{fit}
\usetikzlibrary{matrix}
\tikzset{%
apple/.pic={
    \fill [MaterialBrown] (-1/8,0)  arc (180:120:1 and 3/2) coordinate [pos=3/5] (@)-- ++(1/6,-1/7)  arc (120:180:5/4 and 3/2) -- cycle;
    \fill [MaterialLightGreen500] (0,-9/10)  .. controls ++(180:1/8) and ++(  0:1/4) .. (-1/3,  -1) .. controls ++(180:1/3) and ++(270:1/2) .. (  -1,   0) .. controls ++( 90:1/3) and ++(180:1/3) .. (-1/2, 3/4) .. controls ++(  0:1/8) and ++(135:1/8) .. (   0, 4/7)
    }
    }

\newcommand{\leftdoublequote}{\textcolor{blue}{\scalebox{3}{``}}}

\newcommand{\rightdoublequote}{\textcolor{blue}{\scalebox{3}{''}}}


\usepackage{textcomp}
\usepackage{fontawesome}
\usepackage{tikz,pgfplots}
\usetikzlibrary{shapes.callouts}
\usetikzlibrary{arrows}
\usetikzlibrary{shapes.geometric, positioning}
\usetikzlibrary{matrix,positioning,decorations.pathreplacing,calc}
\usepackage{tikz-dependency}

\pgfplotsset{compat=1.8} % or newest version

\usetikzlibrary{positioning}


\usepackage{bm}
\usepackage{relsize}

\tcbuselibrary{skins, hooks}

\tikzset{basic/.style={draw,fill=MediumBlue!20,text width=1em,text badly centered}}
\tikzset{input/.style={basic,circle}}
\tikzset{weights/.style={basic,rectangle}}
\tikzset{functions/.style={basic,circle,fill=MediumBlue!10}}



\usepackage{listofitems} % for \readlist to create arrays
\tikzstyle{mynode}=[thick,draw=MediumBlue,fill=MediumBlue!20,circle,minimum size=22]


\usepackage{overpic}

%----------------------------------------------------------------------------------------
%	TITLE PAGE
%----------------------------------------------------------------------------------------

\usepackage{tikz-qtree,tikz-qtree-compat}
\usetikzlibrary{tikzmark}
\usetikzlibrary{calc}

\usetikzlibrary{fit}
\tikzset{%
apple/.pic={
    \fill [MaterialBrown] (-1/8,0)  arc (180:120:1 and 3/2) coordinate [pos=3/5] (@)-- ++(1/6,-1/7)  arc (120:180:5/4 and 3/2) -- cycle;
    \fill [MaterialLightGreen500] (0,-9/10)  .. controls ++(180:1/8) and ++(  0:1/4) .. (-1/3,  -1) .. controls ++(180:1/3) and ++(270:1/2) .. (  -1,   0) .. controls ++( 90:1/3) and ++(180:1/3) .. (-1/2, 3/4) .. controls ++(  0:1/8) and ++(135:1/8) .. (   0, 4/7)
}
}
\usepackage{tikz-qtree,tikz-qtree-compat}
\usetikzlibrary{tikzmark}
\usetikzlibrary{calc}

\usetikzlibrary{positioning}

% Define styles
\tikzset{
    box/.style={
        rectangle,
        draw=black,
        fill=blue!20,
        text width=2.2cm,
        text centered,
        minimum height=0.8cm,
        font=\small
    },
    arrow/.style={
        ->,
        >=Stealth,
        line width=1.5pt,
        draw=black
    }
}


\usepackage{bm}
\usepackage{relsize}



\tikzset{basic/.style={draw,fill=MediumBlue!20,text width=1em,text badly centered}}
\tikzset{input/.style={basic,circle}}
\tikzset{weights/.style={basic,rectangle}}
\tikzset{functions/.style={basic,circle,fill=MediumBlue!10}}
\usetikzlibrary{arrows.meta, bending}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit, backgrounds}

\usepackage{listofitems} % for \readlist to create arrays
\tikzstyle{mynode}=[thick,draw=MediumBlue,fill=MediumBlue!20,circle,minimum size=22]


\newmdenv[
backgroundcolor=androidYellowLight,
linecolor=androidYellow,
linewidth=0.5pt,
roundcorner=10pt,
skipabove=\baselineskip,
skipbelow=\baselineskip
]{MintedFrame}

% Set minted options
\setminted{
fontsize=\footnotesize,
breaklines=true,
autogobble,
linenos,
frame=none
}


\title[CPE 487/587: Deep Learning]{CPE 486/586: Deep Learning for Engineering Applications} % The short title appears at the bottom of every slide, the full title is only on the title page
\subtitle{06 Generative Adversarial Network}

\author[Rahul Bhadani] {{\Large \textbf{Rahul Bhadani}}}

\institute[UAH] % Your institution as it will appear on the bottom of every slide, maybe shorthand to save space
{
    Electrical \& Computer Engineering,  The University of Alabama in Huntsville
}
\date

% \titlegraphic{
%    \includegraphics[width=0.4\linewidth]{figures/UAH_primary.png}
% }

\begin{document}

%-------------------------------------------------
\begin{frame}
    \titlepage
\end{frame}

%-------------------------------------------------
\begin{frame}{Outline}
    \backgroundtableofcontents
\end{frame}
    

%------------------------------------------------
\begin{frame}{Lecture Overview}
    \begin{itemize}
        \item In this lecture we discussed generative models (briefly) and how to model distributions of data using density estimation techniques. One advantage to these generative models is that we can generate data from the distribution once we have the model.   
        \item Our goal in this lecture is to introduce {\em \color{MediumRed}Generative Adversarial Networks} (GANs) for generating data from a distribution $p_{data}(\x)$. GANs are able to model complex distributions of data that would be incredible challenging to model using the density estimation approaches discussed in previous lectures. 
        \item Modeling $p_{data}(\x)$ is increasingly challenging for complex data, such as real scenes, people, etc. 
    \end{itemize}
\end{frame}


\section{Motivation}

\begin{frame}
    \sectionpage
\end{frame}

\begin{frame}{Discriminative vs. Generative Models}
    \begin{columns}[T]
        \begin{column}{0.49\textwidth}
            \centering
            \begin{block}{Discriminative}
                \small Focuses on the \textbf{boundary} between classes.
                \vspace{0.5em}
                \begin{itemize}
                    \item Maps high-dimensional input $\mathbf{x}$ to label $y$.
                    \item Learns $P(y \mid \mathbf{x})$.
                    \item \textit{Goal:} Classification and Regression.
                \end{itemize}
            \end{block}
        \end{column}
        
        \begin{column}{0.49\textwidth}
            \centering
            \begin{block}{Generative}
                \small Focuses on the \textbf{distribution} of the data.
                \vspace{0.5em}
                \begin{itemize}
                    \item Models the process that \textit{creates} the data.
                    \item Learns $P(\mathbf{x}, y)$ or $P(\mathbf{x})$.
                    \item \textit{Strategy:} MLE or Variational Inference.
                \end{itemize}
            \end{block}
        \end{column}
    \end{columns}

    \vspace{1.5em}

    \begin{quote}
        \centering
        \small
        While \textbf{Discriminative} models tell you how to tell things apart, \textbf{Generative} models give you the "recipe" for the data itself.
    \end{quote}
    	\centering
    	
    \textbf{	GAN is a generative model.}
    	
\end{frame}



\begin{frame}{Adversarial Machine Learning}

\footnotesize

    \begin{columns}[T]
        \begin{column}{0.7\textwidth}
            \begin{tcolorbox}[colback=blue!5, colframe=blue!75!black, title=Wikipedia Definition, arc=0mm, left=2pt, right=2pt, top=2pt, bottom=2pt]
                 Study of attacks on ML algorithms and defenses against such attacks.
            \end{tcolorbox}

            \begin{itemize}
                \item \textbf{Data Assumption:} Conventional training is based on IID assumption, but however someone can supply non-IID data.
                \item \textbf{Exploitation:} Adversaries may attempt to exploit a learning mechanism either to cause it to misbehave or to extract or misuse information.
            \end{itemize}
            
        \end{column}

        \begin{column}{0.3\textwidth}
            \begin{figure}
                \centering
                \resizebox{0.6\textwidth}{!}{
                    \includegraphics{figures/detective.jpg}
                }
             
            \end{figure}
        \end{column}
    \end{columns}

  \begin{tcolorbox}[colback=red!5, colframe=red!75!black, boxrule=0.5pt]
                \footnotesize While we won't delve in Secure Machine Learning, we serve this as a motivation for \textbf{Generative Adversarial Network (GAN)}.
            \end{tcolorbox}

    \begin{tcolorbox}[colback=green!5, colframe=green!60!black]
         
        GAN is a \textbf{two-player minimax} game where:
        \begin{description}
            \item[Generator:] A counterfeiter (adversary) trying to fool you.
            \item[Discriminator:] A police officer investigating and detecting counterfeit objects.
        \end{description}
    \end{tcolorbox}
\end{frame}


%------------------------------------------------
\begin{frame}{Example of a Complex $p_{data}(\x)$ }
\begin{center}
\uncover<1->{\includegraphics[width=.3\textwidth]{figures/gan-p1.jpg}}
\uncover<2->{\includegraphics[width=.3\textwidth]{figures/gan-p2.jpg}}
\uncover<3->{\includegraphics[width=.3\textwidth]{figures/gan-p3.jpg}}\\
\Large \uncover<4->{\color{MediumRed}None of these people are real!}
\end{center}
\end{frame}



%-----------------------------------------------
\begin{frame}{GANs}
\begin{center}
\includegraphics[width=.4\textwidth]{figures/gans.png}\\
There are hundreds(?) of different types of GANs; however, this lecture only presents the vanilla GAN. 
\end{center}
\end{frame}



\section{Generative Adversarial Networks}

\begin{frame}
    \sectionpage
\end{frame}


\begin{frame}{Definitions}

\footnotesize

    \begin{columns}[T, totalwidth=\textwidth]
        % Left Column: Distributions
        \begin{column}{0.48\textwidth}
            \begin{tcolorbox}[colback=datagreen!5, colframe=datagreen, title=\textbf{Data Distribution}, fonttitle=\bfseries\small, arc=1mm]
                 $p_{data}(\mathbf{x})$: Probability distribution over the data. 
                \begin{itemize}
                    \item Access to $\{\mathbf{x}_i\}_{i=1}^n$
                    \item No labels needed (not classification).
                \end{itemize}
            \end{tcolorbox}
            
            \pause
            \begin{tcolorbox}[colback=gray!5, colframe=gray!60, title=\textbf{Latent Variable}, fonttitle=\bfseries\small, arc=1mm]
                 $p_{g}(\mathbf{z})$: Probability distribution over a latent variable $\mathbf{z}$. Think of this distribution as being over random noise.
            \end{tcolorbox}
        \end{column}

        % Right Column: Networks
        \begin{column}{0.48\textwidth}
            \pause
            \begin{tcolorbox}[colback=genblue!5, colframe=genblue, title=\textbf{Generator} $G(\mathbf{z})$, fonttitle=\bfseries\small, arc=1mm]
                 $G$ is the generator neural network that takes in a noise vector, $\zbf$, to produce a vector $\x_{fake}$. $\x_{fake}$ is a ``fake'' data sample that is generated from $\zbf$. 
            \end{tcolorbox}

            \pause
            \begin{tcolorbox}[colback=discred!5, colframe=discred, title=\textbf{Discriminator} $D(\mathbf{x})$, fonttitle=\bfseries\small, arc=1mm]
                 $D$ takes vector $\mathbf{x}$ and tries to discern if $\mathbf{x}$ was sampled from $p_{data}(\mathbf{x})$ or generated by $G$.
            \end{tcolorbox}
        \end{column}
    \end{columns}

    \pause
    
    % Bottom wide box for parameters
    \begin{tcolorbox}[enhanced, sharp corners, boxrule=0pt, frame hidden, colback=yellow!10, borderline west={2pt}{0pt}{orange}]
         Let $\theta_g$ and $\theta_d$ represent the parameters for the generator and discriminator neural networks, respectively.
    \end{tcolorbox}
\end{frame}


\begin{frame}{Generative Adversarial Net}
    \begin{center}
        \begin{tikzpicture}[scale=0.15]
            \node[circle, draw, thick] (z) {$\zbf$};
            \node[circle, draw, thick, right=5em of z] (x) {$\x_{fake}$};
            \draw[-stealth, thick] (z) -- node[above] {$G(\zbf)$} node[below] {generator} (x);
            \node[left=of z] (i) {};
            \draw[-stealth, thick] (i) -- node[above] {$p_g(\zbf)$} (z);
            \node[above=of x, circle, draw, thick] (xt) {$\x_{real}$};
            \node[left=5em of xt] (it) {};
            \draw[-stealth, thick] (it) -- node[above] {$p_{data}(\x)$} (xt);
            \node[circle, draw, thick, right=5em of x, yshift=2.5em] (D) {$\x$};
            \node[right=7em of D] (out) {real?};
            \draw[-stealth, thick] (D) -- node[above] {$D(\x)$} node[below] {discriminator} (out);
                    
            \node[right=2.5em of x, circle, fill, inner sep=0.15em] (pt1) {};
            \node[right=2.5em of xt, circle, fill, inner sep=0.15em] (pt2) {};
                    
            \draw[dashed, thick] (pt1) edge[bend left] (pt2);
                    
            \node[circle, draw, thick, fill=white, inner sep=0.15em] at ([xshift=-0.9em, yshift=4em]pt1.north) (pt3) {};
                    
            \draw[-stealth, thick] (x) -- (pt1);
            \draw[-stealth, thick] (xt) -- (pt2);
            \draw[-stealth, thick] (pt3) -- (D);
        
        \end{tikzpicture}
        
        \begin{quote}
        Goodfellow, Ian J., Jean Pouget-Abadie, Mehdi Mirza, Bing Xu, David Warde-Farley, Sherjil Ozair, Aaron Courville, and Yoshua Bengio. "Generative adversarial nets." Advances in neural information processing systems 27 (2014).
        \end{quote}
        \end{center}
        
\end{frame}



%------------------------------------------------
\begin{frame}{Generative Adversarial Net}
    \small
\begin{block}{}
\begin{itemize}
    \item We have two neural network in a GAN
    \begin{itemize}
        \item The network $G$'s goal is to generate sample that look like they came from $p_{data}(\x)$. 
        \item The network $D$'s goal is to effectively discriminate between real samples from $p_{data}(\x)$ and fake samples from $G$. 
    \end{itemize}
    \item The goals of $G$ and $D$ are different! We can view this scenario as a game between $D$ and $G$. 
\end{itemize}
\end{block}

\begin{quote}
We train $D$ to maximize the probability of assigning the
correct label to both training examples and samples from $G$. We simultaneously train $G$ to minimize $\log(1 - D(G(z)))$.
\end{quote}


\end{frame}





%------------------------------------------------
\begin{frame}[fragile]{Formally Defining the Optimization Task}

\begin{tblock}{}
The optimization of $\theta_g$ and $\theta_d$ can be cast as a $\min$-$\max$ task, which is given by
\begin{multiequation}
\min_{G}\max_{D} V (D, G) = \left\{ \Ebb_{\xbf\sim p_{data}}\left[ \log(D(\x)) \right] + \Ebb_{\zbf\sim p_{\zbf}}\left[\log(1 - D(G(\zbf)))\right]\right\}
\end{multiequation}

$V$ is called as value function.

\end{tblock}

From the original paper: alternate between $k$ steps of optimizing $D$ and one step of optimizing $G$.

\begin{texample}{}
 
 In the beginning $G$ is poor, so $D$ will reject samples with a high confidence, and 
 $\log(1 - D(G(z)))$ saturates. So, rather than training $G$ to minimize $\log(1 - D(G(z)))$ , $G$ is trained to maximize $\log D(G(z))$.
 
\end{texample}


\end{frame}





%------------------------------------------------
\begin{frame}{Formally Defining the Optimization Task}
    
\begin{block}{}
\begin{itemize}
    \item If $\x \sim p_{data}(\x)$ then the discriminator should maximize $\log D(\x)$
    \item If $\zbf\sim p_g(\zbf)$ then the generator seeks to generate a sample that makes the discriminator think it is a normal sample. 
    \item We can use stochastic gradient descent and ascent to adjust the parameters of the network. 
\end{itemize}
\end{block}

\end{frame}

\begin{frame}{Global Optimality of $p_g = p_{\textbf{data}}$}

If we fix the generator $G$, the optimal discriminator $D$ is

\LARGE 

\begin{align*}
\centering
D^*_G(\xbf) = \frac{p_{data}(\xbf)}{p_{data}(\xbf) + p_g(\xbf)}
\end{align*}

\end{frame}


\begin{frame}{Minimax Equation with Optimal Discriminator}

For a fixed G, the minimax is

\begin{align*}
C(G) & = \max_{D} V (D, G) \\
& =   \Ebb_{\xbf\sim p_{data}}\left[ \log(D_G^*(\x)) \right] + \Ebb_{\zbf\sim p_{\zbf}}\left[\log(1 - D_G^*(G(\zbf)))\right]\\
& =  \Ebb_{\xbf\sim p_{data}}\left[ \log(D_G^*(\x)) \right] + \Ebb_{\xbf\sim p_g}\left[\log(1 - D_G^*(\xbf))\right]\\
& = \Ebb_{\xbf\sim p_{data}}\left[ \log   \frac{p_{data}(\xbf)}{p_{data}(\xbf) + p_g(\xbf)} \right] + \Ebb_{\xbf\sim p_g}\left[\log  \frac{p_{\xbf}(\xbf)}{p_{data}(\xbf) + p_g(\xbf)} \right]
\end{align*}

Here, training objective of $D$ can be interpreted as maximizing the log-likelihood for estimating the conditional probability $P(Y = y|\xbf)$ where $Y$ is the random variable denoting where $\xbf$ comes from $p_{data}$ with $y=1$ or from $p_g$ with $y=0$.

\end{frame}


%------------------------------------------------
\begin{frame}{Formally Defining the Optimization Task}
\begin{alertblock}{}
Theorem 1. The global minimum of the virtual training criterion $C(G)$ is achieved if and only if $p_g = p_{data}$. At that point, $C(G)$ achieves the value $-\log (4)$. This point is a Nash Equilibrium. 
\end{alertblock}

\end{frame}




%------------------------------------------------
%------------------------------------------------
%------------------------------------------------
\begin{frame}{Pseudo Code for Training a GAN}

    \footnotesize
\begin{itemize}
    \item[ ] {\bf for} $t = 1, \ldots, T$
    \begin{itemize}
        \item[ ]  {\bf for} $k = 1, \ldots, K$
        \begin{itemize}
            \item Sample $m$ noise data $\zbf_1, \ldots, \zbf_m$ from $p_g(\zbf)$
            \item Sample $m$ data samples $\xbf_1, \ldots, \xbf_m$ from $p_{data}(\x)$
            \item  Update the discriminator by ascending its stochastic gradient:
            \begin{multiequation}
                \nabla_{\theta_d}\left\{ \frac{1}{m}\sum_{i=1}^m \left[ \log(D(\x_i)) + \log(1 - D(G(\zbf_i))) \right]\right\}
            \end{multiequation}
        \end{itemize}
        \item[ ] {\bf end for}
        \item  Sample $m$ noise data $\zbf_1, \ldots, \zbf_m$ from $p_g(\zbf)$
        \item Update the generator by descending its stochastic gradient:
        \begin{multiequation}
            \nabla_{\theta_g} \left\{  \frac{1}{m}\sum_{i=1}^m \log(1 - D(G(\zbf_i)))  \right\}
        \end{multiequation}
    \end{itemize}
    \item[ ] {\bf end for}
\end{itemize}

\end{frame}





%------------------------------------------------
\begin{frame}{Formally Defining the Optimization Task}

\begin{center}
\includegraphics[height=.7\textheight]{figures/gan-opt.png}\\
From Roger Grosse's GAN lecture notes.
\end{center}

\end{frame}









%------------------------------------------------
\begin{frame}{On Training GANs}
\small

\begin{block}{Challenge}
One issue associated with training a GAN is the form of the loss. Observe above that the generator weights only get updated by the gradients of the second term:
\begin{multiequation}
\log(1 - D(G(\zbf)))
\end{multiequation}\vspace{-2em}
\begin{itemize}
    \item The problem with this is that if the generator sample is really bad, then the discriminator’s prediction is close to zero, and since $\log(1-D)$ is very flat when $D\approx0$ there is not enough ``signal'' to move the generator weights meaningfully. 
    \item Increasing learning rates do not seem to help. This is called the {\em saturation problem} in GANs. 
    \item To fix this, while updating generator weights, it is common to heuristically replace the second term in the GAN loss with $-\log D(G(\zbf))$
\end{itemize}
\end{block}


\end{frame}



%------------------------------------------------
\begin{frame}{On Training GANs}

    \small

\begin{tblock}{Challenge}
Stably training GANs was a challenge faced by the community for quite some time (and continues to be a challenge), and a common resolution is to use {\em Wasserstein GANs. }
\begin{itemize}
    \item The GAN loss is a  form of distance between probability distributions (called the Jensen-Shannon divergence), and this can be generated to other distances. 
    \item A common alternative is the Earth-mover or Wasserstein distance, leading to a different type of GAN model called Wasserstein GAN. The WGAN was shown to improve stability of the optimization process
\end{itemize}
\end{tblock}


\end{frame}



\section{GANs (in PyTorch)}

\begin{frame}
    \sectionpage
\end{frame}


%------------------------------------------------
\begin{frame}[fragile]{Generators}
    \begin{columns}[T]
        % Left Column: Theory and Diagram
        \begin{column}{0.45\textwidth}
            \begin{tcolorbox}[colback=blue!5, colframe=blue!50!black, title=Conceptual Flow, fonttitle=\bfseries\small]
                \footnotesize
                The Generator takes random noise (latent vector $\mathbf{z}$) and generates an $m \times n$ image.
            \end{tcolorbox}

            \vspace{0.5em}
            
            \centering
            \resizebox{\textwidth}{!}{
                \begin{tikzpicture}[
                    node distance=1.2cm,
                    block/.style={draw, rectangle, minimum height=2.5em, minimum width=4.5em, fill=blue!10, font=\sffamily\bfseries},
                    arrow/.style={-{Stealth[scale=1.2]}, thick}
                ]
                    % Nodes
                    \node (noise) [label={[align=center]below:\tiny Latent Vector $\mathbf{z}$\\\tiny (Random Noise)}] {
                        \fbox{\includegraphics[width=1.2cm]{figures/whitenoise.png}}
                    };
                    \node (generator) [block, right=of noise] {Generator};
                    \node (output) [right=of generator, label={[align=center]below:\tiny $m \times n$ Image}] {
                        \fbox{\includegraphics[width=1.2cm]{figures/five.png}}
                    };
            
                    % Connections
                    \draw [arrow] (noise) -- (generator);
                    \draw [arrow] (generator) -- (output);
                \end{tikzpicture}
            }
        \end{column}

        % Right Column: Code Implementation
        \begin{column}{0.53\textwidth}
            \begin{tcolorbox}[enhanced, boxrule=0.5pt, colback=pink!5, colframe=pink!50!black, title=PyTorch Implementation, fonttitle=\bfseries\small]
                \begin{minted}[
                    fontsize=\tiny,
                    breaklines,
                    baselinestretch=1.0
                ]{Python}
class Generator(nn.Module):
    def __init__(self, latent_dim=100, height=28, width=28):
        super(Generator, self).__init__()
        self.latent_dim = latent_dim
        self.height, self.width = height, width
        
        self.model = nn.Sequential(
            nn.Linear(latent_dim, 256),
            nn.LeakyReLU(0.2),
            nn.Linear(256, 512),
            nn.LeakyReLU(0.2),
            nn.Linear(512, 1024),
            nn.LeakyReLU(0.2),
            nn.Linear(1024, width*height),
            nn.Tanh() # Output in [-1, 1]
        )
    
    def forward(self, z):
        img = self.model(z)
        # Reshape to [batch, channels, H, W]
        return img.view(img.size(0), 1, 
                        self.height, self.width)
                \end{minted}
            \end{tcolorbox}
        \end{column}
    \end{columns}
\end{frame}


%------------------------------------------------
\begin{frame}[fragile]{Discriminators}
    \begin{columns}[T]
        % Left Column: Theory and Diagram
        \begin{column}{0.45\textwidth}
            \begin{tcolorbox}[colback=blue!5, colframe=red!50!black, title=Conceptual Flow, fonttitle=\bfseries\small]
                \footnotesize

			The discriminator takes generated image and guess whether it is fake or real through binary classification.                
                
            \end{tcolorbox}

            \vspace{0.5em}
            
            \centering
            \resizebox{\textwidth}{!}{
                \begin{tikzpicture}[
                    node distance=1.2cm,
                    block/.style={draw, rectangle, minimum height=2.5em, minimum width=4.5em, fill=red!10, font=\sffamily\bfseries},
                    arrow/.style={-{Stealth[scale=1.2]}, thick}
                ]
                    % Nodes
                    \node (noise) [label={[align=center]below:\tiny Generate Image}] {
                        \fbox{\includegraphics[width=1.2cm]{figures/five.png}}
                    };
                    \node (discriminator) [block, right=of noise] {Discriminator};
                    \node (output) [right=of discriminator, label={[align=center]below:\tiny Real or Fake}] {
                        \fbox{ \Large 0 / 1}
                    };
            
                    % Connections
                    \draw [arrow] (noise) -- (discriminator);
                    \draw [arrow] (discriminator) -- (output);
                \end{tikzpicture}
            }
        \end{column}

        % Right Column: Code Implementation
        \begin{column}{0.53\textwidth}
            \begin{tcolorbox}[enhanced, boxrule=0.5pt, colback=pink!5, colframe=pink!50!black, title=PyTorch Implementation, fonttitle=\bfseries\small]
                \begin{minted}[
                    fontsize=\tiny,
                    breaklines,
                    baselinestretch=1.0
                ]{Python}
class Discriminator(nn.Module):
    def __init__(self, height  = 28, width = 28):
        super(Discriminator, self).__init__()
        self.height, self.width = height, width
        
        self.model = nn.Sequential(
            nn.Linear(self.height*self.width, 1024),
            nn.LeakyReLU(0.2),
            nn.Dropout(0.3),  # Regularization
            nn.Linear(1024, 512),
            nn.LeakyReLU(0.2),
            nn.Dropout(0.3),
            nn.Linear(512, 256),
            nn.LeakyReLU(0.2),
            nn.Dropout(0.3),
            nn.Linear(256, 1),
            nn.Sigmoid()  # Output probability [0, 1]
        )
    
    def forward(self, img):
        img_flat = img.view(img.size(0), -1)  # Flatten
        validity = self.model(img_flat)
        return validity
                \end{minted}
            \end{tcolorbox}
        \end{column}
    \end{columns}
\end{frame}


\begin{frame}{Training Loss in GAN Implementation}
\scriptsize

\begin{tcolorbox}[colback=blue!5, colframe=theoryblue, arc=2pt]
    Training loss is actually just Binary Cross Entropy loss effectively.
    $$\mathcal{L}_{BCE}(y, \hat{y}) = -[y \cdot \log(\hat{y}) + (1-y) \cdot \log(1-\hat{y})]$$
\end{tcolorbox}

\begin{columns}[t]
    \begin{column}{0.48\textwidth}
        \textbf{Case 1: Real Sample ($y=1$)}
        \begin{tcolorbox}[colback=gray!5, colframe=gray!50, arc=0pt]
            $$\mathcal{L}_{BCE} = -\log D(x)$$
        \end{tcolorbox}
    \end{column}
    \begin{column}{0.48\textwidth}
        \textbf{Case 2: Fake Sample ($y=0$)}
        \begin{tcolorbox}[colback=gray!5, colframe=gray!50, arc=0pt]
            $$\mathcal{L}_{BCE} = -\log(1 - D(G(z)))$$
        \end{tcolorbox}
    \end{column}
\end{columns}

\vspace{0.2cm}
Exactly the negative of the value function mentioned in the original paper. 

\begin{columns}[t]
    \begin{column}{0.48\textwidth}
\begin{tcolorbox}[colback=red!5, colframe=warnred, arc=2pt]
    We also noted earlier that GoodFellow recommended to maximize $\log D(G(z))$ instead of minimizing $\log(1 - D(G(z)))$, becase early training: $D$ easily detects fakes $\Rightarrow D(G(z)) \approx 0$
    $$\frac{\partial}{\partial \theta} \log(1 - D(G(z))) \approx 0 \quad \text{\textbf{(Vanishing gradients!)}}$$
\end{tcolorbox}
    \end{column}
    \begin{column}{0.48\textwidth}
\begin{table}
\centering
\resizebox{0.8\textwidth}{!}{%
\begin{tabular}{@{}cc@{}}
\toprule
\textbf{\textcolor{theoryblue}{Theory (Maximize)}} & \textbf{\textcolor{codegreen}{Code (Minimize)}} \\ \midrule
$\mathbb{E}[\log D(x)]$ & \texttt{criterion(D(x), 1)} \\
$\mathbb{E}[\log(1-D(G(z)))]$ & \texttt{criterion(D(G(z)), 0)} \\ \bottomrule
\end{tabular}}
\end{table}

\begin{center}
    \textbf{\textcolor{blue}{Implementation: Just flip the label!}}
\end{center}

    \end{column}
\end{columns}







\end{frame}






%------------------------------------------------
\begin{frame}{GAN Loss}
\begin{center}
\includegraphics[width=.7\textwidth]{figures/gan-losses.pdf}
\end{center}
\end{frame}




\section{Examples}

\begin{frame}
    \sectionpage
\end{frame}




%------------------------------------------------
%------------------------------------------------



%------------------------------------------------
\begin{frame}{GAN example}
\begin{center}
\includegraphics[width=.3\textwidth]{figures/gan-mnist-0.png}
\includegraphics[width=.3\textwidth]{figures/gan-mnist-400.png}
\includegraphics[width=.3\textwidth]{figures/gan-mnist-8000.png}
\end{center}
\end{frame}


%------------------------------------------------
\begin{frame}{GAN example: Sketch to Figure}
\begin{center}
\includegraphics[width=.6\textwidth]{figures/gan-sketch.png}
\end{center}
\end{frame}


%------------------------------------------------
\begin{frame}{GAN example: Cycle GANs}

\begin{block}{}
\begin{itemize}
\item Image-to-image translation involves generating a new synthetic version of a given image with a specific modification, such as translating a summer landscape to winter
\end{itemize}
\end{block}

\end{frame}



%------------------------------------------------
\begin{frame}{GAN example: Cycle GANs}
\begin{center}
\includegraphics[width=.8\textwidth]{figures/gan-style.png}
\end{center}
\end{frame}


%------------------------------------------------
\begin{frame}{GAN example: Cycle GANs}
\begin{center}
\includegraphics[width=.8\textwidth]{figures/gan-cycle.png}
\end{center}
\end{frame}



%------------------------------------------------
% \begin{frame}{First AI-Generated Portrait Ever Sold at Auction Shatters Expectations}
% \begin{center}
% \uncover<2->{\includegraphics[width=.6\textwidth]{figures/gan-arrested.jpg}}
% \end{center}
% \end{frame}


%------------------------------------------------
\begin{frame}{First AI-Generated Portrait Ever Sold at Auction Shatters Expectations: \$432,500}
\begin{center}
\includegraphics[width=.4\textwidth]{figures/gan-art.jpg}
\end{center}
\end{frame}


%------------------------------------------------
\begin{frame}{Cosmopolitan Meets DALL-E 2} 
\begin{center}
\includegraphics[width=.4\textwidth]{figures/gan-cosmo.png}
\end{center}
\end{frame}



%------------------------------------------------
\begin{frame}{DALL-E 2: Text-to-Image Synthesis}

\begin{block}{}
\begin{itemize}
\item DALL-E 2 is a new AI system that can create realistic images and art from a description in natural language.
DALL-E 2 can create original, realistic images and art from a text description. It can combine concepts, attributes, and styles.
% DALL-E's model is a multimodal implementation of GPT-3[16] with 12 billion parameters[1] which "swaps text for pixels", trained on text-image pairs from the Internet.[17] DALL-E 2 uses 3.5 billion parameters, a smaller number than its predecessor
\item DALL-E's uses multimodal version of GPT-3 with 12 billion parameters. The concept is that it swaps text for pixels and it is trained on trained on text-image pairs from the Internet. DALL-E 2 uses 3.5 billion parameters. 
\end{itemize}
\end{block}

\end{frame}



%------------------------------------------------
\begin{frame}{DALL-E 2} 
\begin{center}
``a painting of a fox sitting
in a field at sunrise in the
style of Claude Monet''\\
\uncover<2->{
\includegraphics[width=.4\textwidth]{figures/dall-e-1.jpg}
\includegraphics[width=.4\textwidth]{figures/dall-e-2.jpg}
}
\end{center}
\end{frame}












%------------------------------------------------
\section{Challenges with GANs}
%------------------------------------------------

\begin{frame}
    \sectionpage
\end{frame}




%------------------------------------------------

\begin{frame}{Issues with GANs (Google's Comments)}
\begin{tblock}{}
\begin{itemize}
    \item Research has suggested that if your discriminator is too good, then generator training can fail due to vanishing gradients. In effect, an optimal discriminator doesn't provide enough information for the generator to make progress.
    \begin{itemize}
        \item The Wasserstein loss is designed to prevent vanishing gradients even when you train the discriminator to optimality. 
    \end{itemize}
    \item The generator may learn to produce a single output. The generator is always trying to find the one output that seems most plausible to the discriminator. This form of GAN failure is called {\em mode collapse}.
    \item GANs frequently fail to converge, as discussed in the module on training.
\end{itemize}
\end{tblock}
\end{frame}


%------------------------------------------------
\section{Advance Architecture in GAN}
%------------------------------------------------

\begin{frame}
    \sectionpage
\end{frame}



\begin{frame}{Deep Convolution GAN}


Fractionally-Strided Convolutional Layer (or Transposed Convolution) is opposite of convolution operation. 

In a convolution operation (for example, stride = 2), we obtain a downsampled (smaller) output of the larger input.

In fractionally-streded convolution,  an upsampled (larger) output is obtained from a smaller input. 

Consider $X = \begin{bmatrix}
x_{00} & x_{01}\\
x_{10} & x_{11}
\end{bmatrix}$ and filter as $K = \begin{bmatrix}
k_{00} & k_{01} & k_{02}\\
k_{10} & k_{11} & k_{12}\\
k_{20} & k_{21} & k_{22}\\
\end{bmatrix}$ 

with stride of 2, i.e. fractional stride of 1/2, we insert, zeros to between every other input pixel. Hence,  $X = \begin{bmatrix}
x_{00} & 0 & x_{01} & 0\\
0 & 0 & 0 & 0\\
x_{10} & 0  x_{11} & 0\\
0 & 0 & 0 & 0\\
\end{bmatrix}$. Then we apply regular convolution operation.


 

\vfill 
\begin{quote}
\tiny Radford, Alec, Luke Metz, and Soumith Chintala. "Unsupervised representation learning with deep convolutional generative adversarial networks." arXiv preprint arXiv:1511.06434 (2015).
\end{quote}
\end{frame}

\begin{frame}{Deep Convolution GAN}
\scriptsize

\begin{tcolorbox}[colback=mainblue!5, colframe=mainblue, arc=3pt, boxrule=0.5pt]
    \textbf{Fractionally-Strided Convolutional Layer} (or Transposed Convolution) is the opposite of the convolution operation.
\end{tcolorbox}

\begin{columns}[t]
    \begin{column}{0.5\textwidth}
        \textbf{Key Concepts:}
        \begin{itemize}
            \item \textbf{Standard Convolution:} In a convolution operation (for example, stride = 2), we obtain a downsampled (smaller) output of the larger input.
            \item \textbf{Fractional Stride:} In fractionally-strided convolution, an upsampled (larger) output is obtained from a smaller input.
        \end{itemize}
        
         
        \begin{tcolorbox}[colback=gray!10, boxrule=0pt, frame hidden, left=2pt, right=2pt, top=2pt, bottom=2pt]
    \tiny \textcolor{gray}{Radford, Alec, Luke Metz, and Soumith Chintala. "Unsupervised representation learning with deep convolutional generative adversarial networks." arXiv preprint arXiv:1511.06434 (2015).}
\end{tcolorbox}

        
      

    \end{column}
    
    \begin{column}{0.48\textwidth}
        \textbf{Mathematical Example:}
        Consider $X = \begin{bmatrix}
        x_{00} & x_{01}\\
        x_{10} & x_{11}
        \end{bmatrix}$ and filter as $K = \begin{bmatrix}
        k_{00} & k_{01} & k_{02}\\
        k_{10} & k_{11} & k_{12}\\
        k_{20} & k_{21} & k_{22}\\
        \end{bmatrix}$ 
       
  \begin{tcolorbox}[colback=lightbg, colframe=gray!50,  fonttitle=\bfseries]
    We insert zeros between every other input pixel. Hence:
    \[
    X = \begin{bmatrix}
    x_{00} & 0 & x_{01} & 0\\
    0 & 0 & 0 & 0\\
    x_{10} & 0 & x_{11} & 0\\
    0 & 0 & 0 & 0\\
    \end{bmatrix}
    \]
    Then we apply regular convolution operation.
\end{tcolorbox}

    \end{column}
\end{columns}



\end{frame}

\begin{frame}{Deep Convolution GAN}

From the original paper:

\begin{enumerate}
\item  Replace any pooling layers with strided convolutions (discriminator) and fractional-strided convolutions (generator).
\item Use batchnorm in both the generator and the discriminator.
\item Remove fully connected hidden layers for deeper architectures.
\item Use ReLU activation in generator for all layers except for the output, which uses Tanh.
\item  Use LeakyReLU activation in the discriminator for all layers.
\end{enumerate}

\vspace{10pt}

\begin{block}{}
The DCGAN made possible vector arithmetic for image maniuplation: vector("King") - vector("Man") + vector("Woman") = vector("Queen").
\end{block}


\end{frame}


\begin{frame}{Cycle GAN Architecture}

It is used for image to image translation to transfer some interesting features from image to another image, as we saw earlier. Examples: Horse to Zebra, Summer to Winter, etc.

Cycle GAN introduces \textbf{Cycle Consistency Loss} in addition to adversarial Loss seen in the original GAN.

Cycle GAN has four networks:

\begin{enumerate}
\item $G_{A\to B}$ : a generator for translating images from domain $ A \to B$.

\item $D_B$: a discriminator if an image is real or fake in $B$.

\item $G_{B\to A}$ : a generator for translating images from domain $ B \to A$.

\item $D_A$: a discriminator if an image is real or fake in $A$.


\end{enumerate}

\end{frame}

%------------------------------------------------
% \begin{frame}{Wasserstein GAN}
% \begin{tblock}{}
% \begin{multiequation}
    
% \end{multiequation}
% \end{tblock}
% \end{frame}

%------------------------------------------------
%------------------------------------------------
%------------------------------------------------
%------------------------------------------------

\begin{frame}{Code}

\LARGE

\centering

\url{https://github.com/rahulbhadani/CPE487587_SP26/blob/master/Code/Chapter_06_Generative_Adversarial_Network.ipynb}

\end{frame}




\begin{frame}{References}
    % Beamer does not support BibTeX so references must be inserted manually as below
    \footnotesize{
        \begin{thebibliography}{99}
            \bibitem[Goodfellow, 2013]{p1} Ian J. Goodfellow, Jean Pouget-Abadie, Mehdi Mirza, Bing Xu, David Warde-Farley, Sherjil Ozair, Aaron Courville, Yoshua Bengio (2013)
            \newblock Generative Adversarial Networks
            \newblock \emph{Advances in Neural Information Processing Systems}.
        \end{thebibliography}
    }
\end{frame}




    %------------------------------------------------
    \begin{frame}
        \Huge{\centerline{\color{MediumBlue}\textbf{The End}}}
    \end{frame}

\end{document}